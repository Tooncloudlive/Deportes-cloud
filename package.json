{
  "name": "tooncloud-deportes",
  "version": "1.0.0",
  "description": "Web de deportes con scraping automatico de partidos y escudos",
  "scripts": {
    "scrape:partidos": "node scripts/scrape-partidos.js",
    "scrape:escudos": "node scripts/scrape-escudos.js",
    "build": "node scripts/build.js",
    "scrape:all": "npm run scrape:partidos && npm run scrape:escudos && npm run build"
  },
  "dependencies": {
    "playwright": "^1.59.1"
  },
  "engines": {
    "node": ">=18"
  }
}
